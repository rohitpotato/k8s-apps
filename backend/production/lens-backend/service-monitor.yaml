apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lens-backend
  namespace: backend
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: lens-backend
  endpoints:
    - port: http
      interval: 30s
      path: /metrics
      scheme: http
