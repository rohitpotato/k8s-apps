apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: lens-frontend
  namespace: frontend
  labels:
    release: kube-prometheus-stack
spec:
  selector:
    matchLabels:
      app: lens-frontend
  endpoints:
    - port: metrics
      interval: 30s
      path: /metrics
      scheme: http